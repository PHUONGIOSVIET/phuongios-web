---
deployment:
  tasks:
    - export DEPLOYPATH=/home/jsphuon/public_html/
    - /bin/cp -R *.html $DEPLOYPATH
    - /bin/cp -R *.css $DEPLOYPATH
    - /bin/cp -R *.js $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
